//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  printWidth: 150,
  tabWidth: 4,
  useTabs: false,
  semi: true,
  singleQuote: true,
  trailingComma: 'none',
  arrowParens: 'always',
  bracketSpacing: true,
}
export default config
